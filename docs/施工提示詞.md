# 施工提示詞集：要做哪項，複製對應提示詞貼上即可

> **使用方法：**
> 1. 開新對話（建議先用 `/model` 切到 Sonnet，省額度）
> 2. 找到你要做的任務，把整段提示詞複製貼上
> 3. 照 AI 的步驟走。它會自動讀到 CLAUDE.md 的規則（不會亂部署、完工會給你驗證清單）
>
> **建議順序：1 → 2 → 3 → 4 → 5 → 6，一次做一項，做完驗證通過再做下一項。**

---

## 任務 1：班表介面 ✅ 已完成（2026-07-08 Fable 覆核通過）

介面已隨「工單2」commit（5bc508b）完成並上線：每週班表與單日調整都改為「時間選擇器＋時段小卡」，存檔格式不變、防呆完整。

剩下的只是**操作**，不用寫程式：後台 →「班表」→ 選調理師 → 每天用時間選擇器加入可約時段 → 按「儲存」。臨時休診用「單日調整」：選日期 → 移除全部時段 → 儲存單日。

---

## 任務 2：LINE Login channel 正式發布

```
我的 LINE Login channel（channel ID 2010520705，LIFF ID 2010520705-60EHkpbz）目前還在開發模式，只有測試者能用。我要把它正式發布（Published），讓所有客戶都能開啟 booking.html 預約頁。

請一步步帶我在 LINE Developers Console 操作：
1. 告訴我發布前要檢查什麼（隱私權政策、服務條款網址等 LINE 要求的欄位）
2. 如果需要準備隱私權政策頁面，幫我產出一份適合經絡調理工作室的簡易版本（繁體中文），並告訴我怎麼放上 GitHub Pages 給 LINE 填網址
3. 帶我完成發布操作
4. 發布後給我一份驗證清單：用一個「非測試者」的 LINE 帳號實測預約流程

我是非工程背景，每一步請講清楚要點哪裡。不要 git push，有檔案要上線時先給我看、我確認才推。
```

---

## 任務 3：LINE 圖文選單

```
我要為 LINE 官方帳號 @741cforc「神庭&神藏｜361經絡調理」設定圖文選單（Rich Menu），讓客戶點選單就能：
1. 開啟預約頁（LIFF 入口：https://liff.line.me/2010520705-60EHkpbz）
2. 查詢自己的方案剩餘次數（也是進 booking.html，看它有沒有這功能，沒有就先告訴我）
3. 查看店家資訊（地址、營業時間）

請帶我完成：
1. 先建議選單版型（幾格、每格放什麼），給我 2-3 個方案選
2. 產出選單底圖的設計規格（尺寸、每格文字），如果你能直接產生一張簡潔的選單圖片就更好
3. 一步步帶我在 LINE Official Account Manager 後台上傳圖片、設定每格的動作連結
4. 給我驗證清單

我是非工程背景，每一步請講清楚要點哪裡。
```

---

## 任務 4：PLAN_CONFIG 單一來源（消除同步風險）

```
請先讀 docs/產品化藍圖.md 的「工單二」，然後執行這項改造：

目前方案規則 PLAN_CONFIG（買10送2 效期1月／買20送6 效期1.5月／買30送10 效期2月）在 index.html（約583-585行）和 booking-worker.js（約28-30行）各寫死一份，哪天只改一邊就會出現前後台不一致的災難。

請把它改成單一來源：存進 Firestore config/app（那裡已經有 pushSecret，讀取模式是現成的），index.html 和 booking-worker.js 開機時讀取，讀不到時 fallback 用現有寫死的值（保險）。

規矩：
- 動工前先 git commit 建還原點
- index.html 改完我自己驗證後才 git push；booking-worker.js 改完把 wrangler deploy 指令準備好給我，我自己執行
- 完工給我 3 步驗證清單（後台顯示的方案次數 ＝ 客戶端顯示 ＝ Worker 計算，三邊一致）
```

---

## 任務 5：點數到期提醒推播（自動）

```
我要做「方案快到期自動提醒」功能：每天自動檢查所有客戶，找出「7 天內到期、而且還有剩餘次數」的方案，透過 LINE 推播提醒客戶回來預約。

背景：我的系統是 index.html（後台）＋ booking.html（客戶端）＋ booking-worker.js（Cloudflare Worker，已有 LINE 推播能力，pushSecret 存在 Firestore config/app）。方案資料含 expiryDate、totalSessions、usedSessions 欄位。

請先提出計畫給我看（這是新功能，照 CLAUDE.md 的 C 級流程），計畫要涵蓋：
1. 建議用 Cloudflare Worker 的 Cron Trigger（wrangler.toml 加排程）每天固定時間跑，如果你有更好的做法請說明比較
2. 提醒訊息文案（親切、繁體中文，署名店家，包含剩餘次數和到期日）
3. 防重複機制：同一個方案不要每天都提醒，最多提醒 1-2 次
4. 只推播給有綁定 lineUserId 的客戶，沒綁定的整理成清單讓我人工聯絡

我同意計畫後才動工。完工後 wrangler deploy 指令準備給我自己執行，並給我驗證方法（例如先用我自己的測試帳號做一個明天到期的假方案試推）。
```

---

## 任務 6：每週營運報表（推播給老闆）

```
我要做「每週營運小報表」：每週一早上 9 點，自動統計上週數據，用 LINE 推播給我自己（調理師），內容包含：
1. 上週預約幾筆（新增／完成／取消或拒絕各幾筆）
2. 完成幾次調理、共扣了幾點
3. 各方案賣出幾組
4. 超過 30 天沒回來消費的客戶名單（只列名字，不列病歷內容）

背景：系統是 index.html（後台）＋ booking-worker.js（Cloudflare Worker，已有 LINE 推播能力）。如果任務 5（到期提醒）已經做好 Cron Trigger，直接共用同一個排程機制。

請先提出計畫（C 級流程），包含：我和另一位調理師的 lineUserId 要存在哪裡、報表只推給我們兩人。我同意後才動工。完工後 wrangler deploy 指令準備給我執行，並附驗證方法。

注意紅線：報表內容不得包含客戶病歷細節，客戶名單只出現稱呼。
```

---

## 任務 7：THERAPISTS 設定抽離（產品化工單一，等系統穩定 1-2 個月再做）

```
請先讀 docs/產品化藍圖.md 的「工單一」和第 3-4 節，然後執行這項改造：

目前調理師代號 glan／xuan 在 index.html 寫死約 50 處、booking.html 約 11 處。為了未來能把系統複製給其他店家（調理師人數／名字不同），要把它抽成檔案頂部的設定物件，例如：
THERAPISTS = { glan: { name: '鈞' }, xuan: { name: '萱' } }
其餘程式碼一律從這個設定讀取，不再寫死。

這是大工程，規矩嚴格執行：
- 先提計畫：列出所有要改的位置分類（下拉選單、班表、預約邏輯、顯示文字…），我同意才動工
- 動工前 git commit 建還原點
- 分批改、每批改完跑語法檢查
- 完工後給我完整回歸驗證清單：兩位調理師的「預約→確認→完成扣點→推播」全流程都要各測一遍
- 我全部驗證通過、明確說好之後，才能 git push
```

---

## 任務 8：設定區塊標準化＋開店手冊（產品化工單三＋四，接在任務 7 之後）

```
請先讀 docs/產品化藍圖.md 的「工單三、工單四」和第 5 節，然後執行：

A. 把 index.html、booking.html、booking-worker.js、wrangler.toml 檔案頂部整理出統一格式的「店家設定區塊」（firebaseConfig、WORKER_URL、LIFF_ID、店名、方案規則指標、調理師設定），目標是：開新店只改這個區塊，其他一行都不用動。

B. 把 SETUP.md 升級成「開店手冊」，照藍圖第 5 節的 SOP 骨架撰寫，標準是「一個沒碰過這系統的人照著做能自己開起來」，每步寫清楚在哪個網站、點哪裡。

規矩：A 動到已上線檔案，先 git commit 還原點、完工附驗證清單、我確認才 push。B 純文件，寫完給我看即可。
```

---

## 備用任務：沉睡客戶名單（後台功能，隨時可做）

```
我要在 index.html 後台加一個「沉睡客戶」區塊：列出超過 45 天沒有完成調理紀錄的客戶（名字＋最後一次調理日期＋剩餘點數），讓我決定要不要主動關心。不要自動發訊息給客戶，只做清單顯示。

規矩：動工前 git commit 還原點，完工附 3 步驗證清單，我確認才 push。
```

---

## 每週檢視提示詞（建議每週固定一天貼一次，10 分鐘搞定）

```
請幫我做本週進度檢視。步驟：
1. 讀 docs/產品化藍圖.md 第 9 節的施工順序，和 docs/施工提示詞.md 的任務清單
2. 用 git log 看最近的 commit，判斷哪些任務已完成、哪些做到一半
3. 用白話回報我：
   - 已完成：哪幾項（各一句話）
   - 進行中：停在哪一步、下一步是什麼
   - 有沒有「改了本機但還沒上線」的檔案（git status 檢查，這種半吊子狀態最危險）
4. 建議我下週只做「一項」主線任務，說明為什麼是它
5. 如果發現我連續兩週卡在同一項，提醒我考慮：換個做法、切回 Fable 處理、或先跳過做別項

只做檢視和建議，不要動任何檔案。
```

---

## 遇到狀況時的萬用提示詞

卡關、出錯、AI 鬼打牆時貼這段：

```
停下來，不要再嘗試。請閱讀 .claude/harness/03_rubrics.md 判斷現在該停手還是該問我，然後：
1. 用白話總結你目前做了什麼、卡在哪
2. 列出你已經排除的可能原因
3. 告訴我現在系統處於什麼狀態（線上有沒有被影響？本機檔案改了哪些？）
4. 給我 2-3 個選項（含「回滾到還原點」這個選項）讓我決定
```
